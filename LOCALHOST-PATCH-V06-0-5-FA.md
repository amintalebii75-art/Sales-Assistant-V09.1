# نصب نسخه V06.0.5 روی localhost

## ۱. پوشه جدید را مستقل نگه دارید

پوشه را داخل `C:\xampp\htdocs` قرار دهید:

`C:\xampp\htdocs\Sales-Assistant-V06-0-5-Team-Results-Handoff-Font`

نسخه V06.0.4 را فعلاً حذف نکنید.

## ۲. فایل تنظیمات را کپی کنید

از نسخه V06.0.4 فایل `config.php` را کپی و داخل پوشه V06.0.5 قرار دهید.

## ۳. دیتابیس

همان دیتابیس قبلی استفاده می‌شود. هیچ فایل SQL یا Migration جدیدی اجرا نکنید.

## ۴. فونت

برای فعال‌شدن کامل فونت، فایل‌های زیر را که خودتان در اختیار دارید داخل پوشه `assets\fonts` کپی کنید:

- `IRANSansXFaNum-Regular.ttf`
- `IRANSansXFaNum-Medium.ttf`
- `IRANSansXFaNum-DemiBold.ttf`
- `IRANSansXFaNum-Bold.ttf`

## ۵. مدیر اولیه

اگر حساب مدیر قبلاً ساخته شده، فایل `create_admin.php` را در نسخه جدید حذف کنید.

## ۶. آدرس اجرا

`http://localhost:8080/Sales-Assistant-V06-0-5-Team-Results-Handoff-Font/login.php`

## تست پیشنهادی

1. با حساب مدیر وارد شوید.
2. در «ثبت مذاکره و فعالیت» دکمه `+` کنار نتایج را بزنید.
3. یک گزینه آزمایشی بسازید.
4. با حساب مرکز تماس وارد شوید؛ گزینه باید دیده و قابل انتخاب باشد، ولی دکمه `+` نباید دیده شود.
5. یک مشتری دارای بازاریاب مسئول را انتخاب و نتیجه را ثبت کنید.
6. با حساب همان بازاریاب وارد شوید؛ یک وظیفه ارجاعی از مرکز تماس باید در «وظایف» دیده شود.
