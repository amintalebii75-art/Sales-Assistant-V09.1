# CHANGELOG V06.0.4 — Jalali Date Selectors

## Scope
تمام ورودی‌های تاریخ قابل ویرایش در CRM و Planning به انتخابگر شمسی تبدیل شدند. ذخیره تاریخ‌های روزانه همچنان به‌صورت ISO `YYYY-MM-DD` در Backend و دیتابیس انجام می‌شود.

## Changes
- انتخاب روز، ماه و سال شمسی برای تاریخ شروع/پایان هفته و سررسید وظیفه در برنامه ماهانه.
- انتخاب ماه و سال شمسی برای ایجاد و کپی برنامه ماهانه.
- `month_key` جدید به‌صورت شمسی مانند `1405-05` ذخیره می‌شود؛ کلیدهای قدیمی میلادی مانند `2026-07` همچنان پذیرفته و در UI به شمسی نمایش داده می‌شوند.
- نمایش تاریخ‌ها و زمان‌ها با تقویم صریح Persian/Jalali.
- افزایش بازه سال در انتخابگرهای فعلی CRM و حفظ سال رکوردهای قدیمی.
- هیچ Migration دیتابیسی لازم نیست؛ ستون‌های DATE همچنان میلادی/ISO باقی مانده‌اند.

## Security/Data boundary
- CSRF، RBAC، Revision و APIهای Planning تغییر ماهوی نکرده‌اند.
- تبدیل تاریخ فقط در UI انجام می‌شود و Backend همچنان تاریخ ISO معتبر را بررسی می‌کند.
