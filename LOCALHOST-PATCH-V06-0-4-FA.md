# نصب Patch تاریخ شمسی V06.0.4 روی localhost

این بسته یک نسخه مستقل است. پوشه قبلی را overwrite نکنید.

1. پوشه را در `C:\xampp\htdocs\Sales-Assistant-V06-0-4-Jalali-Date-Selectors` قرار دهید.
2. فایل `config.php` نسخه در حال اجرای خود را به پوشه جدید کپی کنید.
3. برای ادامه با داده‌های فعلی، همان `db_name` قبلی را نگه دارید.
4. Migration جدیدی لازم نیست.
5. از مسیر `planning.php` ایجاد برنامه، ویرایش هفته و تاریخ سررسید وظیفه را تست کنید.

نکته: تاریخ‌های روزانه در دیتابیس به ISO میلادی ذخیره می‌شوند، ولی تمام انتخاب و نمایش کاربر شمسی است. این کار مقایسه، مرتب‌سازی و Queryهای دیتابیس را پایدار نگه می‌دارد.
