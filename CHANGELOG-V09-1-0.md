# تغییرات V09.1.0 — فرم‌های قابل تنظیم و اطلاعات پایه

## هدف
این نسخه امکان پیکربندی کنترل‌شده فرم‌های CRM را بدون ساخت یک فرم‌ساز نامحدود فراهم می‌کند. مدیر می‌تواند فرم مشتری و فرم ثبت مذاکره را متناسب با فرایند شرکت ساده یا کامل کند، درحالی‌که RBAC و سقف دسترسی `View / Call / Edit` در Backend ثابت می‌ماند.

## قابلیت‌های اضافه‌شده

- صفحه مدیریتی جدید: **فرم‌ها و اطلاعات پایه**.
- روشن یا خاموش‌کردن فیلدهای از پیش تعریف‌شده.
- اجباری یا اختیاری‌کردن فیلدها.
- تعیین نمایش هر فیلد برای مدیر، بازاریاب و مرکز تماس.
- فیلدهای قفل‌شده سیستمی که قابل حذف نیستند:
  - نام مشتری
  - انتخاب مشتری در مذاکره
  - نتیجه مذاکره
- فهرست‌های اطلاعات پایه مشترک:
  - نحوه آشنایی
  - زمینه فعالیت
  - گروه محصولات
  - نوع مصرف
  - بسته‌بندی
  - نوع ارز
  - ارتباط برای
  - مسیر ارتباط
- افزودن گزینه با دکمه `+` در فرم‌ها.
- سه سیاست افزودن گزینه:
  - فقط مدیر
  - پیشنهاد کاربر و تأیید مدیر
  - افزودن مستقیم کاربر
- تأیید، رد، تغییر نام و غیرفعال‌کردن گزینه‌ها توسط مدیر.
- حفظ داده‌های قدیمی هنگام مخفی یا غیرفعال‌شدن فیلد/گزینه.
- نمایش اطلاعات پایه مشتری در پرونده مشتری.
- نمایش اطلاعات پایه مذاکره در جزئیات مذاکره.
- Server-side validation برای تنظیمات فرم و اطلاعات پایه.
- حذف ارتقای ضمنی سطح دسترسی مرکز تماس از `view` به `call`.
- به‌روزرسانی PWA cache به V09.1.0.

## مرزهای امنیتی

- تنظیم ظاهری یک فیلد، Permission جدید ایجاد نمی‌کند.
- مرکز تماس فقط با Customer Access برابر `call` یا `edit` می‌تواند مذاکره ثبت کند.
- فیلدهای مالی/سفارش برای مرکز تماس همچنان در Backend مسدود هستند.
- گزینه‌های Pending برای کاربران عادی نمایش داده نمی‌شوند.
- فقط مدیر دارای `settings.manage` و `state.save_full` می‌تواند تنظیمات سازمانی فرم را تغییر دهد.

## پایگاه داده

Migration یا SQL جدید لازم نیست. تنظیمات در همان `app_state` و همراه Revision و Backup ذخیره می‌شوند.
